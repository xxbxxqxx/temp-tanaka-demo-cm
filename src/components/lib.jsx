import * as React from "react";
import PropTypes from "prop-types";
import { Link } from "gatsby";

const Lib = ({ siteTitle }) => (
  ''
);

Lib.propTypes = {
}

Lib.defaultProps = {
}

export default Lib
